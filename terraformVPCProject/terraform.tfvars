vpc_cidr_block         = "10.0.0.0/16"   # Custom CIDR block for your VPC
vpc_name               = "custom-vpc-module"  # Name of the VPC (changed to custom-vpc-module)
public_subnet_cidr     = "10.0.1.0/24"    # CIDR block for Public Subnet
private_subnet_cidr    = "10.0.2.0/24"    # CIDR block for Private Subnet
public_subnet_az       = "ap-south-1a"    # Availability Zone for Public Subnet (Mumbai region)
private_subnet_az      = "ap-south-1b"    # Availability Zone for Private Subnet (Mumbai region)
aws_region = "ap-south-1"  # Mumbai region
